# Building Block Roadmap Report

Current scope:

- Backend menu item.
- OWL client action.
- Loads active EA layers.
- Multi-layer dropdown filter with checkboxes.
- Automatic debounced RPC search when selected layers change.
- Temporary fake rows only.

Next scope:

- Add `ea.component` records under each `ea.layer`.
- Link each component to an `ir.model` record.
- Search inside selected `ea.entity.*` models.
- Add date range, pagination, SVG rendering, PDF and Excel later.
